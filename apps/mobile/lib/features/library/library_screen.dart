import 'package:flutter/material.dart';
class LibraryScreen extends StatelessWidget { const LibraryScreen({super.key}); @override Widget build(BuildContext context)=>Scaffold(appBar:AppBar(title:const Text('Library')),body:ListView(padding:const EdgeInsets.all(16),children:const [ListTile(leading:Icon(Icons.folder_outlined),title:Text('Collections'),subtitle:Text('Group documents into reusable AI contexts')),ListTile(leading:Icon(Icons.sell_outlined),title:Text('Tags')),ListTile(leading:Icon(Icons.history),title:Text('Recent conversations')),ListTile(leading:Icon(Icons.download_done),title:Text('Offline content'))])); }
