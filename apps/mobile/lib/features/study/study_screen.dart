import 'package:flutter/material.dart';
class StudyScreen extends StatelessWidget { const StudyScreen({super.key}); @override Widget build(BuildContext context)=>DefaultTabController(length: 3, child: Scaffold(appBar: AppBar(title: const Text('Study'), bottom: const TabBar(tabs: [Tab(text:'Flashcards'),Tab(text:'Quizzes'),Tab(text:'Notes')])), body: TabBarView(children: [_Cards(),_Quizzes(),_Notes()]))); }
class _Cards extends StatelessWidget { @override Widget build(BuildContext context)=>ListView(padding: const EdgeInsets.all(16), children:[Card(child: Padding(padding: const EdgeInsets.all(24), child: Column(crossAxisAlignment: CrossAxisAlignment.start, children:[const Text('DUE TODAY'),const SizedBox(height:12),Text('24 cards',style:Theme.of(context).textTheme.headlineSmall?.copyWith(fontWeight:FontWeight.bold)),const SizedBox(height:16),FilledButton(onPressed:(){},child:const Text('Start review'))])))]); }
class _Quizzes extends StatelessWidget { @override Widget build(BuildContext context)=>const Center(child:Text('Create and review cited quizzes')); }
class _Notes extends StatelessWidget { @override Widget build(BuildContext context)=>const Center(child:Text('Offline-cached notes')); }
